@props(['name', 'email'])

<span class="block text-sm font-semibold text-gray-900 dark:text-white">{{ $name }}</span>
<span class="block truncate text-sm text-gray-900 dark:text-white">{{ $email }}</span>